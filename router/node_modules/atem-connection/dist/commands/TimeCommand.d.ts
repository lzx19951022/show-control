/// <reference types="node" />
import AbstractCommand from './AbstractCommand';
import { AtemState } from '../state';
import { TimeInfo } from '../state/info';
import * as Enums from '../enums';
export declare class TimeCommand extends AbstractCommand {
    rawName: string;
    properties: TimeInfo;
    serialize(): Buffer;
    deserialize(rawCommand: Buffer): void;
    applyToState(state: AtemState): string;
}
export declare class TimeRequestCommand extends AbstractCommand {
    rawName: string;
    minimumVersion: Enums.ProtocolVersion;
    properties: {};
    serialize(): Buffer;
}
