/// <reference types="node" />
import AbstractCommand from '../AbstractCommand';
import { AtemState } from '../../state';
import { AudioMasterChannel } from '../../state/audio';
export declare class AudioMixerMasterCommand extends AbstractCommand {
    static MaskFlags: {
        gain: number;
        balance: number;
        followFadeToBlack: number;
    };
    rawName: string;
    properties: Partial<AudioMasterChannel>;
    serialize(): Buffer;
}
export declare class AudioMixerMasterUpdateCommand extends AbstractCommand {
    rawName: string;
    properties: Partial<AudioMasterChannel>;
    deserialize(rawCommand: Buffer): void;
    applyToState(state: AtemState): string;
}
