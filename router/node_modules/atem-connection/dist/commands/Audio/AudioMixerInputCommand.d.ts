/// <reference types="node" />
import AbstractCommand from '../AbstractCommand';
import { AtemState } from '../../state';
import { AudioChannel } from '../../state/audio';
export declare class AudioMixerInputCommand extends AbstractCommand {
    static MaskFlags: {
        mixOption: number;
        gain: number;
        balance: number;
    };
    rawName: string;
    mixEffect: number;
    properties: Partial<AudioChannel>;
    index: number;
    serialize(): Buffer;
}
export declare class AudioMixerInputUpdateCommand extends AbstractCommand {
    rawName: string;
    mixEffect: number;
    properties: Partial<AudioChannel>;
    index: number;
    deserialize(rawCommand: Buffer): void;
    applyToState(state: AtemState): string;
}
