/// <reference types="node" />
import AbstractCommand from './AbstractCommand';
import { AtemState } from '../state';
export declare class TallyBySourceCommand extends AbstractCommand {
    rawName: string;
    auxBus: number;
    properties: {
        [source: number]: {
            program: boolean;
            preview: boolean;
        } | undefined;
    };
    deserialize(rawCommand: Buffer): void;
    applyToState(_state: AtemState): never[];
}
