/// <reference types="node" />
import AbstractCommand from '../../AbstractCommand';
import { AtemState } from '../../../state';
export declare class FadeToBlackRateCommand extends AbstractCommand {
    rawName: string;
    mixEffect: number;
    properties: {
        rate: number;
    };
    serialize(): Buffer;
}
export declare class FadeToBlackRateUpdateCommand extends AbstractCommand {
    rawName: string;
    mixEffect: number;
    properties: {
        rate: number;
    };
    deserialize(rawCommand: Buffer): void;
    applyToState(state: AtemState): string;
}
